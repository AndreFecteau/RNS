#ifndef MATH_H
#define MATH_H

namespace math{

  /////////////////////////////////////////////////////////////////////////
  /// \brief
  /// \param
  template <typename T>
  T Power(const T& num, const int& expo) {return pow(num, expo);}

} // namespace math
#endif //#ifndef MATH_H
